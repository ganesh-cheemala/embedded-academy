# KCI Academy Live Branding Fix

This release fixes the live Render database branding left over from the
pre-KCI deployment.

Changes:
- Existing `academy_name = Embedded Academy` is migrated to `KCI Academy`.
- Header/admin logo fallback changes from `EA` to `KCI`.
- FastAPI version is updated to 19.0.

GitHub and Render service names are intentionally unchanged.
